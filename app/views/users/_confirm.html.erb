<div class="modal fade" id="confirm-modal" tabindex="-1" role="dialog" aria-labelledby="confirm-modal" aria-hidden="true">
  <div class="modal-dialog modal-dialog-centered" role="document">
    <div class="modal-content">
      <div class="modal-header border-0">
        <h5 class="modal-title" id="confirm-modal-title">　退会確認</h5>
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
      <div class="modal-body mx-auto">
        退会すると保存したレシピやメニューが削除されます。<br>
        退会する場合はパスワードを入力してください。
        <%= form_with model: @user, url: user_registration_path, method: :delete, local: true do |f| %>
          <div class="mt-2 mx-auto d-table"><%= f.text_field :current_password %></div>
      </div>
      <div class="modal-footer border-0 mx-auto">
          <%= f.submit "退会する", class: "btn btn-sm btn-info" %>
        <% end %>
        <button type="button" class="btn btn-sm btn-secondary" data-dismiss="modal">キャンセル</button>
      </div>
    </div>
  </div>
</div>